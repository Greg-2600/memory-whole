services:
  memory_whole:
    build: .
    ports:
      - "4747:4747"
    volumes:
      - mw_output:/app/output
    restart: unless-stopped
    environment:
      - PORT=4747
      - FORCE_REGEN=0

volumes:
  mw_output:
    name: mw_output
